# FitBuddy-AI Setup Guide

## 1. Requirements

Make sure the following are installed:

- Python 3.11 or newer
- VS Code
- Git (optional)

## 2. Open the Project

Open the `FitBuddy-AI` folder in VS Code.

## 3. Create Virtual Environment

Open the VS Code terminal and run:

```powershell
python -m venv .venv